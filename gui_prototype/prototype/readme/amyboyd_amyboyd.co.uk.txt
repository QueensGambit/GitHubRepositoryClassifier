b"My personal website\n===================\n\nTo serve locally: use a simple HTTP server (e.g. NPM's `http-server dist/`).\n\nWhen editing the blog, set Jekyll to recompile the blog when files change: `jekyll b -w`\n\nTo deploy: `bin/deploy`\n\nTo build assets and watch for changes: `grunt`\n\nThe actual page content is kept in a symlink'ed directory (`src/html/content`) so it can be tweaked outside of source control.\n"